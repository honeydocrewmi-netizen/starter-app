name: Supabase keepalive

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Ping Supabase (no-op if not configured yet)
        # Use the owner project committed in supabase/config.json.
        run: node scripts/keepalive.mjs
